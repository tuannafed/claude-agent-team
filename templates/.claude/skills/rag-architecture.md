# Skill: RAG Architecture

**Used by:** AI Engineer agent when building retrieval-augmented generation (chunking, embeddings, retrieval).

---

## Scope

- Chunking strategy (size, overlap, semantic boundaries).
- Embedding model and vector store choice.
- Retrieval: top-k, reranking, hybrid search.
- Context window and citation handling.

## Conventions

- Document chunking and retrieval in the track’s AI Engineer Output.
- Align embedding dimensions and index config with the chosen stack.
- Note limits (context length, latency, cost) in the track.

See MANIFEST.md for when this skill is copied (AI/LLM team type or `--ai`).
